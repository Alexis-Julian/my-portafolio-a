---
import NavBar from '@components/NavBar.astro';
/* interface Props {
   section: {
      home: boolean;
      proyect: boolean;
      aboutme: boolean;
   };
}
const { section } = Astro.props; */
---

<header class="fixed h-[100%] w-[10%] p-1 right-0 bottom-0 flex items-center">
   <NavBar />
</header>
